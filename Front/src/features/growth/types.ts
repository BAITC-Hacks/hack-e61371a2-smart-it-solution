export type Goal = { targetRole: string; targetGrade: string };
export type RoleProfile = { role: string; grade: string };
export type Skill = { id: string; name: string };
export type EventItem = { eventId: string; title: string; durationHours: number; format: string; mandatory: boolean; isActive: boolean };
export type Preferences = { weeklyHours: number; formats: string[] };
export type Gain = { skillId: string; from: number; to: number };
export type Gap = { skillId: string; requiredLevel: number; currentLevel: number; gap: number; isCritical: boolean };
export type Step = { position?: number; eventId: string; title?: string; sessionId: string | null; scheduledDate?: string; durationHours?: number; gains: Gain[]; prerequisites?: { skillId: string; minLevel: number }[]; alternatives?: string[]; completed?: boolean };
export type Forecast = Partial<Goal> & { baselineLevels: Record<string, number>; projectedLevels: Record<string, number>; unmetRequirements: Gap[]; steps: Step[]; totalHours?: number; budgetHours?: number; startDate?: string; endDate?: string; persisted?: false };
export type PlanSummary = Goal & { id: string; horizonDays: number; status: string; totalHours: number; version: number; createdAt: string };
export type Plan = PlanSummary & Forecast & Preferences & { employeeId: string; startDate: string; goalChanged: boolean; requiresRefresh: boolean };
export type Mentor = { employeeId: string; fullName?: string; headline: string; skillIds: string[]; capacity: number; activeMentees?: number; enabled?: boolean; approvedAt?: string | null };
export type MentorReview = { employee_id: string; full_name: string; headline: string; skill_ids: string[]; capacity: number; enabled: boolean; approved_at: string | null };
export type Mentorship = { id: string; menteeId: string; mentorId: string; menteeName: string; mentorName: string; message: string; status: string; createdAt: string };
export type Task = { id: string; title: string; skillId: string | null; targetLevel: number | null; dueDate: string | null; status: string };
export type Achievements = { visibility: string; achievements: { id: string; title: string; target: number; current: number; achieved: boolean }[]; goalReady: boolean; pointsAwarded: number };
export type Colleague = { employeeId: string; fullName: string; department: string };
export type Recognition = { id: string; senderId: string; recipientId: string; senderName?: string; recipientName?: string; message: string; status: string; createdAt: string };
export type Challenge = { id: string; title: string; description: string; startsOn: string; endsOn: string; status: string; joined: boolean; memberCount: number; completedMembers: number; events: { eventId: string; title: string; completed: boolean }[]; canArchive?: boolean; createdBy?: string };
export type Reward = { id: string; title: string; description: string; cost: number; stock: number | null; active?: boolean };
export type Redemption = { id: string; title: string; cost: number; status: string; createdAt: string; employeeId?: string; fullName?: string };
export type Wallet = { enabled: boolean; balance: number; spendableBalance: number; rules: string; pointsPerEvent: number; monthlyCap: number; ledger: { id: string; delta: number; kind: string; eventId: string | null; createdAt: string }[]; redemptions: Redemption[] };
export type Policy = { enabled: boolean; pointsPerEvent: number; monthlyCap: number; rulesText: string };
